# Uma linha por app principal. Linhas vazias e linhas iniciadas com `#` são ignoradas.
# A ordem é respeitada pelo instalador.
# Componentes declarados do setup ficam em `config/components.sh`.

zen-browser-bin
google-chrome
shellcheck
code
discord
spotify-launcher
steam
