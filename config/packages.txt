# Uma linha por item. Linhas vazias e linhas iniciadas com `#` são ignoradas.
# A ordem é respeitada pelo instalador.
# `codex` é um item especial: ele executa a configuração do Codex CLI.
# `yay` não entra na lista: ele é preparado automaticamente pelo script e usado como helper AUR preferencial.

zen-browser-bin
google-chrome
git
shellcheck
nodejs
npm
codex
code
discord
spotify-launcher
steam
