# Uma linha por item. Linhas vazias e linhas iniciadas com `#` são ignoradas.
# A ordem é respeitada pelo instalador.
# `codex` é um item especial: ele executa a configuração do Codex CLI.
# `yay` não entra na lista: ele é instalado automaticamente antes do primeiro pacote AUR, se necessário.

zen-browser-bin
google-chrome
git
shellcheck
nodejs
npm
codex
code
discord
spotify-launcher
steam
